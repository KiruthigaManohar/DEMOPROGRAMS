#include<stdio.h>
#include<conio.h>
void main()
{
int a,n,cal,i=1;
printf(Enter the base);
scanf("%d",&a);
printf("Enter the exponent");
scanf("%d",&n);
if(i<=n)
{
cal=cal*a;
}
printf("The power of a number=%d",cal);
}
